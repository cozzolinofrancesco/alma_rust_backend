[package]
name = "alma-infrastructure"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
alma-domain = { path = "../domain" }
alma-application = { path = "../application" }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
async-trait = { workspace = true }
tokio = { workspace = true }
futures-core = { workspace = true }
futures-util = { workspace = true }
uuid = { workspace = true }
# Gemini generation + File Search retrieval + corpus ingestion adapters speak
# raw HTTP (rustls-tls, no native OpenSSL C build — plan C11). File Search uses
# the raw X-Goog-Upload-* header protocol, so no `multipart` feature is pulled.
reqwest = { workspace = true }
